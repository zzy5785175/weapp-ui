<ui-transition
    visible="{{ visible }}"
    custom-class="ui-overlay"
    custom-style="z-index: {{ zIndex }}; {{ mask ? 'background: rgba(0, 0, 0, .7)' : '' }}"
    bind:tap="handleClick"
    catch:touchmove="noop"
/>